// Shared constants for freight calculation — safe to import in client components
export const VOLUME_DIVISOR = 6000;   // L × W × H (cm³) ÷ 6000 = volume weight (kg)
export const VOLUME_EXCESS_RATE = 4;  // $4 CAD per kg for volumetric excess over gross weight
